services:
  - type: web
    name: chatbot-web
    env: node
    plan: starter
    buildCommand: npm install && NODE_OPTIONS=--max-old-space-size=384 npm run build:web
    startCommand: node backend/server.js
    healthCheckPath: /health
    autoDeploy: true