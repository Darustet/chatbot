services:
  - type: web
    name: chatbot-web
    env: node
    plan: starter
    buildCommand: CI=1 RENDER=1 npm install
    startCommand: node backend/server.js
    healthCheckPath: /health
    autoDeploy: true